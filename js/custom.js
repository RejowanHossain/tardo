$('.toggle_btn').on('click', function(e) {
  $('.toggle-area').toggleClass("d-none"); //you can list several class names 
  // e.preventDefault();
});

$('.emoji-btn').on('click', function(e) {
  $('.emoji-box').toggleClass("d-none"); //you can list several class names 
  // e.preventDefault();
});
